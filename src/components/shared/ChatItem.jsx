import React from 'react'
import { Link } from '../styles/StyledComponents'

const ChatItem = ({
    avatar=[],
    name,
    _id,
    lastmessage,
    newmessage,
    isonline,
    samesender,
    index=0,
    handleseletechatopen,
    groupchat=false,
}) => {
  return (
    <Link to={"/"}></Link>
  )
}

export default ChatItem